[
  {
    "description": "Check that TrenchBroom is reachable and report which map is open.",
    "inputSchema": {
      "properties": {},
      "required": [],
      "type": "object"
    },
    "name": "ping",
    "readOnly": true
  },
  {
    "description": "Summarise the open map: layers, groups, brush counts, entity classname counts, world bounds, grid size and the current selection. Pass 'into' with a layer or group name to list that container's actual contents instead.",
    "inputSchema": {
      "properties": {
        "into": {
          "description": "Name of a layer or group: returns its children, described one by one, rather than the map-wide summary.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "get_scene",
    "readOnly": true
  },
  {
    "description": "Create an axis-aligned box brush in the current layer.",
    "inputSchema": {
      "properties": {
        "bounds": {
          "description": "An axis-aligned box in world units. Corners may be given in either order.",
          "properties": {
            "max": {
              "description": "The opposite corner, as [x, y, z].",
              "items": {
                "type": "number"
              },
              "maxItems": 3,
              "minItems": 3,
              "type": "array"
            },
            "min": {
              "description": "One corner, as [x, y, z].",
              "items": {
                "type": "number"
              },
              "maxItems": 3,
              "minItems": 3,
              "type": "array"
            }
          },
          "required": [
            "min",
            "max"
          ],
          "type": "object"
        },
        "material": {
          "description": "Material to apply to every face. Defaults to the editor's current material.",
          "type": "string"
        }
      },
      "required": [
        "bounds"
      ],
      "type": "object"
    },
    "name": "create_brush",
    "readOnly": false
  },
  {
    "description": "Create a cylinder inscribed in the given bounds. Pass 'thickness' to make it a hollow tube instead, which produces a ring of brushes.",
    "inputSchema": {
      "properties": {
        "axis": {
          "description": "Axis the shape is revolved around. Defaults to \"z\" (upright).",
          "enum": [
            "x",
            "y",
            "z"
          ],
          "type": "string"
        },
        "bounds": {
          "description": "An axis-aligned box in world units. Corners may be given in either order.",
          "properties": {
            "max": {
              "description": "The opposite corner, as [x, y, z].",
              "items": {
                "type": "number"
              },
              "maxItems": 3,
              "minItems": 3,
              "type": "array"
            },
            "min": {
              "description": "One corner, as [x, y, z].",
              "items": {
                "type": "number"
              },
              "maxItems": 3,
              "minItems": 3,
              "type": "array"
            }
          },
          "required": [
            "min",
            "max"
          ],
          "type": "object"
        },
        "circle": {
          "description": "How the cross-section is approximated. Defaults to 8 edge-aligned sides.",
          "properties": {
            "alignment": {
              "description": "\"edge\" puts a flat face on the axes, \"vertex\" puts a corner there, \"scalable\" subdivides so the shape survives non-uniform scaling.",
              "enum": [
                "edge",
                "vertex",
                "scalable"
              ],
              "type": "string"
            },
            "precision": {
              "description": "Subdivision level, for scalable alignment.",
              "minimum": 0,
              "type": "integer"
            },
            "sides": {
              "description": "Number of sides, for edge and vertex alignment.",
              "minimum": 3,
              "type": "integer"
            }
          },
          "type": "object"
        },
        "material": {
          "description": "Material to apply to every face. Defaults to the editor's current material.",
          "type": "string"
        },
        "thickness": {
          "description": "Wall thickness. Omit for a solid cylinder.",
          "exclusiveMinimum": 0,
          "type": "number"
        }
      },
      "required": [
        "bounds"
      ],
      "type": "object"
    },
    "name": "create_cylinder",
    "readOnly": false
  },
  {
    "description": "Create a cone inscribed in the given bounds, tapering along the given axis.",
    "inputSchema": {
      "properties": {
        "axis": {
          "description": "Axis the shape is revolved around. Defaults to \"z\" (upright).",
          "enum": [
            "x",
            "y",
            "z"
          ],
          "type": "string"
        },
        "bounds": {
          "description": "An axis-aligned box in world units. Corners may be given in either order.",
          "properties": {
            "max": {
              "description": "The opposite corner, as [x, y, z].",
              "items": {
                "type": "number"
              },
              "maxItems": 3,
              "minItems": 3,
              "type": "array"
            },
            "min": {
              "description": "One corner, as [x, y, z].",
              "items": {
                "type": "number"
              },
              "maxItems": 3,
              "minItems": 3,
              "type": "array"
            }
          },
          "required": [
            "min",
            "max"
          ],
          "type": "object"
        },
        "circle": {
          "description": "How the cross-section is approximated. Defaults to 8 edge-aligned sides.",
          "properties": {
            "alignment": {
              "description": "\"edge\" puts a flat face on the axes, \"vertex\" puts a corner there, \"scalable\" subdivides so the shape survives non-uniform scaling.",
              "enum": [
                "edge",
                "vertex",
                "scalable"
              ],
              "type": "string"
            },
            "precision": {
              "description": "Subdivision level, for scalable alignment.",
              "minimum": 0,
              "type": "integer"
            },
            "sides": {
              "description": "Number of sides, for edge and vertex alignment.",
              "minimum": 3,
              "type": "integer"
            }
          },
          "type": "object"
        },
        "material": {
          "description": "Material to apply to every face. Defaults to the editor's current material.",
          "type": "string"
        }
      },
      "required": [
        "bounds"
      ],
      "type": "object"
    },
    "name": "create_cone",
    "readOnly": false
  },
  {
    "description": "Create a sphere inscribed in the given bounds. 'uv' builds it from stacked rings, 'ico' from subdivided triangles with more even faces.",
    "inputSchema": {
      "properties": {
        "axis": {
          "description": "Axis the shape is revolved around. Defaults to \"z\" (upright).",
          "enum": [
            "x",
            "y",
            "z"
          ],
          "type": "string"
        },
        "bounds": {
          "description": "An axis-aligned box in world units. Corners may be given in either order.",
          "properties": {
            "max": {
              "description": "The opposite corner, as [x, y, z].",
              "items": {
                "type": "number"
              },
              "maxItems": 3,
              "minItems": 3,
              "type": "array"
            },
            "min": {
              "description": "One corner, as [x, y, z].",
              "items": {
                "type": "number"
              },
              "maxItems": 3,
              "minItems": 3,
              "type": "array"
            }
          },
          "required": [
            "min",
            "max"
          ],
          "type": "object"
        },
        "circle": {
          "description": "How the cross-section is approximated. Defaults to 8 edge-aligned sides.",
          "properties": {
            "alignment": {
              "description": "\"edge\" puts a flat face on the axes, \"vertex\" puts a corner there, \"scalable\" subdivides so the shape survives non-uniform scaling.",
              "enum": [
                "edge",
                "vertex",
                "scalable"
              ],
              "type": "string"
            },
            "precision": {
              "description": "Subdivision level, for scalable alignment.",
              "minimum": 0,
              "type": "integer"
            },
            "sides": {
              "description": "Number of sides, for edge and vertex alignment.",
              "minimum": 3,
              "type": "integer"
            }
          },
          "type": "object"
        },
        "iterations": {
          "description": "Subdivision count, for kind \"ico\". Defaults to 1. Each step multiplies the face count by four, so keep it low.",
          "minimum": 0,
          "type": "integer"
        },
        "kind": {
          "description": "Defaults to \"uv\".",
          "enum": [
            "uv",
            "ico"
          ],
          "type": "string"
        },
        "material": {
          "description": "Material to apply to every face. Defaults to the editor's current material.",
          "type": "string"
        },
        "rings": {
          "description": "Horizontal ring count, for kind \"uv\". Defaults to 8.",
          "minimum": 1,
          "type": "integer"
        }
      },
      "required": [
        "bounds"
      ],
      "type": "object"
    },
    "name": "create_sphere",
    "readOnly": false
  },
  {
    "description": "Create a semicircular arch as a band of wedge brushes. It springs from the bottom of the bounds and rises to fill them; 'axis' is the direction the opening runs through, as for a tunnel.",
    "inputSchema": {
      "properties": {
        "axis": {
          "description": "Axis the shape is revolved around. Defaults to \"z\" (upright).",
          "enum": [
            "x",
            "y",
            "z"
          ],
          "type": "string"
        },
        "bounds": {
          "description": "An axis-aligned box in world units. Corners may be given in either order.",
          "properties": {
            "max": {
              "description": "The opposite corner, as [x, y, z].",
              "items": {
                "type": "number"
              },
              "maxItems": 3,
              "minItems": 3,
              "type": "array"
            },
            "min": {
              "description": "One corner, as [x, y, z].",
              "items": {
                "type": "number"
              },
              "maxItems": 3,
              "minItems": 3,
              "type": "array"
            }
          },
          "required": [
            "min",
            "max"
          ],
          "type": "object"
        },
        "circle": {
          "description": "How the cross-section is approximated. Defaults to 8 edge-aligned sides.",
          "properties": {
            "alignment": {
              "description": "\"edge\" puts a flat face on the axes, \"vertex\" puts a corner there, \"scalable\" subdivides so the shape survives non-uniform scaling.",
              "enum": [
                "edge",
                "vertex",
                "scalable"
              ],
              "type": "string"
            },
            "precision": {
              "description": "Subdivision level, for scalable alignment.",
              "minimum": 0,
              "type": "integer"
            },
            "sides": {
              "description": "Number of sides, for edge and vertex alignment.",
              "minimum": 3,
              "type": "integer"
            }
          },
          "type": "object"
        },
        "material": {
          "description": "Material to apply to every face. Defaults to the editor's current material.",
          "type": "string"
        },
        "thickness": {
          "description": "Wall thickness of the band.",
          "exclusiveMinimum": 0,
          "type": "number"
        }
      },
      "required": [
        "bounds",
        "thickness"
      ],
      "type": "object"
    },
    "name": "create_arch",
    "readOnly": false
  },
  {
    "description": "Move objects by a delta. Acts on what this call has created so far, or on the editor's selection.",
    "inputSchema": {
      "properties": {
        "delta": {
          "description": "Offset in world units, as [x, y, z].",
          "items": {
            "type": "number"
          },
          "maxItems": 3,
          "minItems": 3,
          "type": "array"
        },
        "target": {
          "description": "What to act on. \"created\" is everything earlier operations in this same call produced, \"last\" is only what the previous operation produced, \"selection\" is what is selected in the editor, and \"auto\" (the default) means created-if-any, otherwise the selection. Use \"last\" to act on one earlier result, such as subtracting only the cutting brush from walls built in the same batch.",
          "enum": [
            "auto",
            "created",
            "last",
            "selection"
          ],
          "type": "string"
        }
      },
      "required": [
        "delta"
      ],
      "type": "object"
    },
    "name": "translate",
    "readOnly": false
  },
  {
    "description": "Rotate objects around an axis, in degrees, counter-clockwise looking down that axis. Defaults to spinning about the centre of what is being rotated.",
    "inputSchema": {
      "properties": {
        "angle": {
          "description": "Angle in degrees.",
          "type": "number"
        },
        "axis": {
          "description": "Axis the shape is revolved around. Defaults to \"z\" (upright).",
          "enum": [
            "x",
            "y",
            "z"
          ],
          "type": "string"
        },
        "center": {
          "description": "Point to rotate around. Defaults to the centre of the objects' bounds.",
          "items": {
            "type": "number"
          },
          "maxItems": 3,
          "minItems": 3,
          "type": "array"
        },
        "target": {
          "description": "What to act on. \"created\" is everything earlier operations in this same call produced, \"last\" is only what the previous operation produced, \"selection\" is what is selected in the editor, and \"auto\" (the default) means created-if-any, otherwise the selection. Use \"last\" to act on one earlier result, such as subtracting only the cutting brush from walls built in the same batch.",
          "enum": [
            "auto",
            "created",
            "last",
            "selection"
          ],
          "type": "string"
        }
      },
      "required": [
        "angle"
      ],
      "type": "object"
    },
    "name": "rotate",
    "readOnly": false
  },
  {
    "description": "Scale objects, either by factors about a centre, or by fitting them into a target box. Give one of 'factors' or 'bounds'.",
    "inputSchema": {
      "properties": {
        "bounds": {
          "description": "An axis-aligned box in world units. Corners may be given in either order.",
          "properties": {
            "max": {
              "description": "The opposite corner, as [x, y, z].",
              "items": {
                "type": "number"
              },
              "maxItems": 3,
              "minItems": 3,
              "type": "array"
            },
            "min": {
              "description": "One corner, as [x, y, z].",
              "items": {
                "type": "number"
              },
              "maxItems": 3,
              "minItems": 3,
              "type": "array"
            }
          },
          "required": [
            "min",
            "max"
          ],
          "type": "object"
        },
        "center": {
          "description": "Fixed point for 'factors'. Defaults to the centre of the objects' bounds.",
          "items": {
            "type": "number"
          },
          "maxItems": 3,
          "minItems": 3,
          "type": "array"
        },
        "factors": {
          "description": "Scale factors as [x, y, z], or one number to scale uniformly.",
          "oneOf": [
            {
              "type": "number"
            },
            {
              "items": {
                "type": "number"
              },
              "maxItems": 3,
              "minItems": 3,
              "type": "array"
            }
          ]
        },
        "target": {
          "description": "What to act on. \"created\" is everything earlier operations in this same call produced, \"last\" is only what the previous operation produced, \"selection\" is what is selected in the editor, and \"auto\" (the default) means created-if-any, otherwise the selection. Use \"last\" to act on one earlier result, such as subtracting only the cutting brush from walls built in the same batch.",
          "enum": [
            "auto",
            "created",
            "last",
            "selection"
          ],
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "scale",
    "readOnly": false
  },
  {
    "description": "Merge brushes into their convex hull. The result is one brush, so this only makes sense for shapes that are actually convex together.",
    "inputSchema": {
      "properties": {
        "target": {
          "description": "What to act on. \"created\" is everything earlier operations in this same call produced, \"last\" is only what the previous operation produced, \"selection\" is what is selected in the editor, and \"auto\" (the default) means created-if-any, otherwise the selection. Use \"last\" to act on one earlier result, such as subtracting only the cutting brush from walls built in the same batch.",
          "enum": [
            "auto",
            "created",
            "last",
            "selection"
          ],
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "csg_merge",
    "readOnly": false
  },
  {
    "description": "Carve the target brushes out of every brush they touch, then delete them. This is how you cut a doorway or window: build a box where the hole should be, then subtract it. The tool brush does not survive the operation.",
    "inputSchema": {
      "properties": {
        "target": {
          "description": "What to act on. \"created\" is everything earlier operations in this same call produced, \"last\" is only what the previous operation produced, \"selection\" is what is selected in the editor, and \"auto\" (the default) means created-if-any, otherwise the selection. Use \"last\" to act on one earlier result, such as subtracting only the cutting brush from walls built in the same batch.",
          "enum": [
            "auto",
            "created",
            "last",
            "selection"
          ],
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "csg_subtract",
    "readOnly": false
  },
  {
    "description": "Replace the target brushes with the volume they all share. Needs at least two, and produces nothing if they do not overlap.",
    "inputSchema": {
      "properties": {
        "target": {
          "description": "What to act on. \"created\" is everything earlier operations in this same call produced, \"last\" is only what the previous operation produced, \"selection\" is what is selected in the editor, and \"auto\" (the default) means created-if-any, otherwise the selection. Use \"last\" to act on one earlier result, such as subtracting only the cutting brush from walls built in the same batch.",
          "enum": [
            "auto",
            "created",
            "last",
            "selection"
          ],
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "csg_intersect",
    "readOnly": false
  },
  {
    "description": "Turn solid brushes into hollow shells with walls of the given thickness, replacing each brush with the six that enclose it. A quick way to make a sealed room from a single box.",
    "inputSchema": {
      "properties": {
        "target": {
          "description": "What to act on. \"created\" is everything earlier operations in this same call produced, \"last\" is only what the previous operation produced, \"selection\" is what is selected in the editor, and \"auto\" (the default) means created-if-any, otherwise the selection. Use \"last\" to act on one earlier result, such as subtracting only the cutting brush from walls built in the same batch.",
          "enum": [
            "auto",
            "created",
            "last",
            "selection"
          ],
          "type": "string"
        },
        "thickness": {
          "description": "Wall thickness in world units. Must be a power of two between 0.125 and 256, because it is applied through the editor's grid. Defaults to the current grid size.",
          "exclusiveMinimum": 0,
          "type": "number"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "csg_hollow",
    "readOnly": false
  },
  {
    "description": "Mirror objects across a plane through their centre, or through an explicit point.",
    "inputSchema": {
      "properties": {
        "axis": {
          "description": "Axis the shape is revolved around. Defaults to \"z\" (upright).",
          "enum": [
            "x",
            "y",
            "z"
          ],
          "type": "string"
        },
        "center": {
          "description": "Point to mirror about. Defaults to the objects' centre.",
          "items": {
            "type": "number"
          },
          "maxItems": 3,
          "minItems": 3,
          "type": "array"
        },
        "select": {
          "description": "Finds objects already in the map. All given fields must match. Resolved fresh on every call, so undo and redo cannot invalidate it.",
          "properties": {
            "all": {
              "description": "Every object in the map.",
              "type": "boolean"
            },
            "bounds": {
              "description": "An axis-aligned box in world units. Corners may be given in either order.",
              "properties": {
                "max": {
                  "description": "The opposite corner, as [x, y, z].",
                  "items": {
                    "type": "number"
                  },
                  "maxItems": 3,
                  "minItems": 3,
                  "type": "array"
                },
                "min": {
                  "description": "One corner, as [x, y, z].",
                  "items": {
                    "type": "number"
                  },
                  "maxItems": 3,
                  "minItems": 3,
                  "type": "array"
                }
              },
              "required": [
                "min",
                "max"
              ],
              "type": "object"
            },
            "classname": {
              "description": "Entities with this classname, and the brushes belonging to them. A trailing * matches a prefix, e.g. \"func_*\".",
              "type": "string"
            },
            "group": {
              "description": "The group with this name, matched as a whole. Trailing * matches a prefix.",
              "type": "string"
            },
            "layer": {
              "description": "Objects in the named layer.",
              "type": "string"
            },
            "material": {
              "description": "Brushes with at least one face using this material. A trailing * matches a prefix.",
              "type": "string"
            },
            "mode": {
              "description": "How 'bounds' matches: fully inside it (default) or merely overlapping it.",
              "enum": [
                "contained",
                "touching"
              ],
              "type": "string"
            }
          },
          "type": "object"
        },
        "target": {
          "description": "What to act on. \"created\" is everything earlier operations in this same call produced, \"last\" is only what the previous operation produced, \"selection\" is what is selected in the editor, and \"auto\" (the default) means created-if-any, otherwise the selection. Use \"last\" to act on one earlier result, such as subtracting only the cutting brush from walls built in the same batch.",
          "enum": [
            "auto",
            "created",
            "last",
            "selection"
          ],
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "flip",
    "readOnly": false
  },
  {
    "description": "Slide the face of the objects' bounding box that faces along 'axis' by 'delta', slanting them.",
    "inputSchema": {
      "properties": {
        "axis": {
          "description": "Axis the shape is revolved around. Defaults to \"z\" (upright).",
          "enum": [
            "x",
            "y",
            "z"
          ],
          "type": "string"
        },
        "delta": {
          "description": "How far to slide that side, as [x, y, z].",
          "items": {
            "type": "number"
          },
          "maxItems": 3,
          "minItems": 3,
          "type": "array"
        },
        "select": {
          "description": "Finds objects already in the map. All given fields must match. Resolved fresh on every call, so undo and redo cannot invalidate it.",
          "properties": {
            "all": {
              "description": "Every object in the map.",
              "type": "boolean"
            },
            "bounds": {
              "description": "An axis-aligned box in world units. Corners may be given in either order.",
              "properties": {
                "max": {
                  "description": "The opposite corner, as [x, y, z].",
                  "items": {
                    "type": "number"
                  },
                  "maxItems": 3,
                  "minItems": 3,
                  "type": "array"
                },
                "min": {
                  "description": "One corner, as [x, y, z].",
                  "items": {
                    "type": "number"
                  },
                  "maxItems": 3,
                  "minItems": 3,
                  "type": "array"
                }
              },
              "required": [
                "min",
                "max"
              ],
              "type": "object"
            },
            "classname": {
              "description": "Entities with this classname, and the brushes belonging to them. A trailing * matches a prefix, e.g. \"func_*\".",
              "type": "string"
            },
            "group": {
              "description": "The group with this name, matched as a whole. Trailing * matches a prefix.",
              "type": "string"
            },
            "layer": {
              "description": "Objects in the named layer.",
              "type": "string"
            },
            "material": {
              "description": "Brushes with at least one face using this material. A trailing * matches a prefix.",
              "type": "string"
            },
            "mode": {
              "description": "How 'bounds' matches: fully inside it (default) or merely overlapping it.",
              "enum": [
                "contained",
                "touching"
              ],
              "type": "string"
            }
          },
          "type": "object"
        },
        "target": {
          "description": "What to act on. \"created\" is everything earlier operations in this same call produced, \"last\" is only what the previous operation produced, \"selection\" is what is selected in the editor, and \"auto\" (the default) means created-if-any, otherwise the selection. Use \"last\" to act on one earlier result, such as subtracting only the cutting brush from walls built in the same batch.",
          "enum": [
            "auto",
            "created",
            "last",
            "selection"
          ],
          "type": "string"
        }
      },
      "required": [
        "delta"
      ],
      "type": "object"
    },
    "name": "shear",
    "readOnly": false
  },
  {
    "description": "Select objects in the editor by a query. Useful on its own to show the user what you mean, and as a first step before acting on the selection.",
    "inputSchema": {
      "properties": {
        "select": {
          "description": "Finds objects already in the map. All given fields must match. Resolved fresh on every call, so undo and redo cannot invalidate it.",
          "properties": {
            "all": {
              "description": "Every object in the map.",
              "type": "boolean"
            },
            "bounds": {
              "description": "An axis-aligned box in world units. Corners may be given in either order.",
              "properties": {
                "max": {
                  "description": "The opposite corner, as [x, y, z].",
                  "items": {
                    "type": "number"
                  },
                  "maxItems": 3,
                  "minItems": 3,
                  "type": "array"
                },
                "min": {
                  "description": "One corner, as [x, y, z].",
                  "items": {
                    "type": "number"
                  },
                  "maxItems": 3,
                  "minItems": 3,
                  "type": "array"
                }
              },
              "required": [
                "min",
                "max"
              ],
              "type": "object"
            },
            "classname": {
              "description": "Entities with this classname, and the brushes belonging to them. A trailing * matches a prefix, e.g. \"func_*\".",
              "type": "string"
            },
            "group": {
              "description": "The group with this name, matched as a whole. Trailing * matches a prefix.",
              "type": "string"
            },
            "layer": {
              "description": "Objects in the named layer.",
              "type": "string"
            },
            "material": {
              "description": "Brushes with at least one face using this material. A trailing * matches a prefix.",
              "type": "string"
            },
            "mode": {
              "description": "How 'bounds' matches: fully inside it (default) or merely overlapping it.",
              "enum": [
                "contained",
                "touching"
              ],
              "type": "string"
            }
          },
          "type": "object"
        }
      },
      "required": [
        "select"
      ],
      "type": "object"
    },
    "name": "select_objects",
    "readOnly": false
  },
  {
    "description": "Create an entity from the game's own definitions. Point entities such as info_player_start or light take an 'origin'; brush entities such as func_door take over the targeted brushes instead. Use list_entity_definitions to see what the current game offers.",
    "inputSchema": {
      "properties": {
        "classname": {
          "description": "Classname as defined by the game, e.g. \"info_player_start\".",
          "type": "string"
        },
        "origin": {
          "description": "Position, for point entities.",
          "items": {
            "type": "number"
          },
          "maxItems": 3,
          "minItems": 3,
          "type": "array"
        },
        "properties": {
          "description": "Extra key/value properties to set on the new entity, e.g. {\"light\": \"300\"}.",
          "type": "object"
        },
        "select": {
          "description": "Finds objects already in the map. All given fields must match. Resolved fresh on every call, so undo and redo cannot invalidate it.",
          "properties": {
            "all": {
              "description": "Every object in the map.",
              "type": "boolean"
            },
            "bounds": {
              "description": "An axis-aligned box in world units. Corners may be given in either order.",
              "properties": {
                "max": {
                  "description": "The opposite corner, as [x, y, z].",
                  "items": {
                    "type": "number"
                  },
                  "maxItems": 3,
                  "minItems": 3,
                  "type": "array"
                },
                "min": {
                  "description": "One corner, as [x, y, z].",
                  "items": {
                    "type": "number"
                  },
                  "maxItems": 3,
                  "minItems": 3,
                  "type": "array"
                }
              },
              "required": [
                "min",
                "max"
              ],
              "type": "object"
            },
            "classname": {
              "description": "Entities with this classname, and the brushes belonging to them. A trailing * matches a prefix, e.g. \"func_*\".",
              "type": "string"
            },
            "group": {
              "description": "The group with this name, matched as a whole. Trailing * matches a prefix.",
              "type": "string"
            },
            "layer": {
              "description": "Objects in the named layer.",
              "type": "string"
            },
            "material": {
              "description": "Brushes with at least one face using this material. A trailing * matches a prefix.",
              "type": "string"
            },
            "mode": {
              "description": "How 'bounds' matches: fully inside it (default) or merely overlapping it.",
              "enum": [
                "contained",
                "touching"
              ],
              "type": "string"
            }
          },
          "type": "object"
        },
        "target": {
          "description": "What to act on. \"created\" is everything earlier operations in this same call produced, \"last\" is only what the previous operation produced, \"selection\" is what is selected in the editor, and \"auto\" (the default) means created-if-any, otherwise the selection. Use \"last\" to act on one earlier result, such as subtracting only the cutting brush from walls built in the same batch.",
          "enum": [
            "auto",
            "created",
            "last",
            "selection"
          ],
          "type": "string"
        }
      },
      "required": [
        "classname"
      ],
      "type": "object"
    },
    "name": "create_entity",
    "readOnly": false
  },
  {
    "description": "List the entity classnames the current game defines, split into point and brush entities. Read these from the game's FGD files, so they reflect the actual game.",
    "inputSchema": {
      "properties": {
        "filter": {
          "description": "Only classnames containing this substring.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "list_entity_definitions",
    "readOnly": true
  },
  {
    "description": "Run one of TrenchBroom's own menu or shortcut actions by name, for anything the other tools do not cover. Actions take no arguments and act on the current selection. Use list_actions to find one.",
    "inputSchema": {
      "properties": {
        "path": {
          "description": "Action path as reported by list_actions, e.g. \"Menu/Edit/Delete\".",
          "type": "string"
        }
      },
      "required": [
        "path"
      ],
      "type": "object"
    },
    "name": "invoke_action",
    "readOnly": false
  },
  {
    "description": "List TrenchBroom's named actions and whether each is currently enabled.",
    "inputSchema": {
      "properties": {
        "filter": {
          "description": "Only actions whose path or label contains this substring.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "list_actions",
    "readOnly": true
  },
  {
    "description": "Render a view of the map as a PNG. The orthographic views read as plans and elevations and are usually easier to reason about than the 3D view.",
    "inputSchema": {
      "properties": {
        "fit": {
          "description": "What to frame: the whole map (default), the selection, or leave the camera where the user left it.",
          "enum": [
            "map",
            "selection",
            "current"
          ],
          "type": "string"
        },
        "view": {
          "description": "\"top\" is a floor plan, \"front\" and \"side\" are elevations. Defaults to \"top\".",
          "enum": [
            "top",
            "front",
            "side",
            "3d"
          ],
          "type": "string"
        },
        "width": {
          "description": "Image width in pixels. Defaults to 768.",
          "maximum": 2048,
          "minimum": 64,
          "type": "integer"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "capture_viewport",
    "readOnly": true
  },
  {
    "description": "Change the material or UV mapping of existing brush faces: retexture, or set the offset, rotation and scale. Values are absolute, not deltas.",
    "inputSchema": {
      "properties": {
        "material": {
          "description": "New material name.",
          "type": "string"
        },
        "rotation": {
          "description": "UV rotation in degrees.",
          "type": "number"
        },
        "select": {
          "description": "Finds objects already in the map. All given fields must match. Resolved fresh on every call, so undo and redo cannot invalidate it.",
          "properties": {
            "all": {
              "description": "Every object in the map.",
              "type": "boolean"
            },
            "bounds": {
              "description": "An axis-aligned box in world units. Corners may be given in either order.",
              "properties": {
                "max": {
                  "description": "The opposite corner, as [x, y, z].",
                  "items": {
                    "type": "number"
                  },
                  "maxItems": 3,
                  "minItems": 3,
                  "type": "array"
                },
                "min": {
                  "description": "One corner, as [x, y, z].",
                  "items": {
                    "type": "number"
                  },
                  "maxItems": 3,
                  "minItems": 3,
                  "type": "array"
                }
              },
              "required": [
                "min",
                "max"
              ],
              "type": "object"
            },
            "classname": {
              "description": "Entities with this classname, and the brushes belonging to them. A trailing * matches a prefix, e.g. \"func_*\".",
              "type": "string"
            },
            "group": {
              "description": "The group with this name, matched as a whole. Trailing * matches a prefix.",
              "type": "string"
            },
            "layer": {
              "description": "Objects in the named layer.",
              "type": "string"
            },
            "material": {
              "description": "Brushes with at least one face using this material. A trailing * matches a prefix.",
              "type": "string"
            },
            "mode": {
              "description": "How 'bounds' matches: fully inside it (default) or merely overlapping it.",
              "enum": [
                "contained",
                "touching"
              ],
              "type": "string"
            }
          },
          "type": "object"
        },
        "target": {
          "description": "What to act on. \"created\" is everything earlier operations in this same call produced, \"last\" is only what the previous operation produced, \"selection\" is what is selected in the editor, and \"auto\" (the default) means created-if-any, otherwise the selection. Use \"last\" to act on one earlier result, such as subtracting only the cutting brush from walls built in the same batch.",
          "enum": [
            "auto",
            "created",
            "last",
            "selection"
          ],
          "type": "string"
        },
        "x_offset": {
          "type": "number"
        },
        "x_scale": {
          "type": "number"
        },
        "y_offset": {
          "type": "number"
        },
        "y_scale": {
          "type": "number"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "set_face_attributes",
    "readOnly": false
  },
  {
    "description": "Group objects under a name. The named group is then a stable handle: select it with {\"group\": name} to move or copy all of it together.",
    "inputSchema": {
      "properties": {
        "name": {
          "type": "string"
        },
        "select": {
          "description": "Finds objects already in the map. All given fields must match. Resolved fresh on every call, so undo and redo cannot invalidate it.",
          "properties": {
            "all": {
              "description": "Every object in the map.",
              "type": "boolean"
            },
            "bounds": {
              "description": "An axis-aligned box in world units. Corners may be given in either order.",
              "properties": {
                "max": {
                  "description": "The opposite corner, as [x, y, z].",
                  "items": {
                    "type": "number"
                  },
                  "maxItems": 3,
                  "minItems": 3,
                  "type": "array"
                },
                "min": {
                  "description": "One corner, as [x, y, z].",
                  "items": {
                    "type": "number"
                  },
                  "maxItems": 3,
                  "minItems": 3,
                  "type": "array"
                }
              },
              "required": [
                "min",
                "max"
              ],
              "type": "object"
            },
            "classname": {
              "description": "Entities with this classname, and the brushes belonging to them. A trailing * matches a prefix, e.g. \"func_*\".",
              "type": "string"
            },
            "group": {
              "description": "The group with this name, matched as a whole. Trailing * matches a prefix.",
              "type": "string"
            },
            "layer": {
              "description": "Objects in the named layer.",
              "type": "string"
            },
            "material": {
              "description": "Brushes with at least one face using this material. A trailing * matches a prefix.",
              "type": "string"
            },
            "mode": {
              "description": "How 'bounds' matches: fully inside it (default) or merely overlapping it.",
              "enum": [
                "contained",
                "touching"
              ],
              "type": "string"
            }
          },
          "type": "object"
        },
        "target": {
          "description": "What to act on. \"created\" is everything earlier operations in this same call produced, \"last\" is only what the previous operation produced, \"selection\" is what is selected in the editor, and \"auto\" (the default) means created-if-any, otherwise the selection. Use \"last\" to act on one earlier result, such as subtracting only the cutting brush from walls built in the same batch.",
          "enum": [
            "auto",
            "created",
            "last",
            "selection"
          ],
          "type": "string"
        }
      },
      "required": [
        "name"
      ],
      "type": "object"
    },
    "name": "create_group",
    "readOnly": false
  },
  {
    "description": "Add an empty named layer. Use move_to_layer to put objects in it.",
    "inputSchema": {
      "properties": {
        "name": {
          "type": "string"
        }
      },
      "required": [
        "name"
      ],
      "type": "object"
    },
    "name": "create_layer",
    "readOnly": false
  },
  {
    "description": "Rename a layer.",
    "inputSchema": {
      "properties": {
        "layer": {
          "description": "Current name.",
          "type": "string"
        },
        "name": {
          "description": "New name.",
          "type": "string"
        }
      },
      "required": [
        "layer",
        "name"
      ],
      "type": "object"
    },
    "name": "rename_layer",
    "readOnly": false
  },
  {
    "description": "Move objects into a named layer.",
    "inputSchema": {
      "properties": {
        "layer": {
          "type": "string"
        },
        "select": {
          "description": "Finds objects already in the map. All given fields must match. Resolved fresh on every call, so undo and redo cannot invalidate it.",
          "properties": {
            "all": {
              "description": "Every object in the map.",
              "type": "boolean"
            },
            "bounds": {
              "description": "An axis-aligned box in world units. Corners may be given in either order.",
              "properties": {
                "max": {
                  "description": "The opposite corner, as [x, y, z].",
                  "items": {
                    "type": "number"
                  },
                  "maxItems": 3,
                  "minItems": 3,
                  "type": "array"
                },
                "min": {
                  "description": "One corner, as [x, y, z].",
                  "items": {
                    "type": "number"
                  },
                  "maxItems": 3,
                  "minItems": 3,
                  "type": "array"
                }
              },
              "required": [
                "min",
                "max"
              ],
              "type": "object"
            },
            "classname": {
              "description": "Entities with this classname, and the brushes belonging to them. A trailing * matches a prefix, e.g. \"func_*\".",
              "type": "string"
            },
            "group": {
              "description": "The group with this name, matched as a whole. Trailing * matches a prefix.",
              "type": "string"
            },
            "layer": {
              "description": "Objects in the named layer.",
              "type": "string"
            },
            "material": {
              "description": "Brushes with at least one face using this material. A trailing * matches a prefix.",
              "type": "string"
            },
            "mode": {
              "description": "How 'bounds' matches: fully inside it (default) or merely overlapping it.",
              "enum": [
                "contained",
                "touching"
              ],
              "type": "string"
            }
          },
          "type": "object"
        },
        "target": {
          "description": "What to act on. \"created\" is everything earlier operations in this same call produced, \"last\" is only what the previous operation produced, \"selection\" is what is selected in the editor, and \"auto\" (the default) means created-if-any, otherwise the selection. Use \"last\" to act on one earlier result, such as subtracting only the cutting brush from walls built in the same batch.",
          "enum": [
            "auto",
            "created",
            "last",
            "selection"
          ],
          "type": "string"
        }
      },
      "required": [
        "layer"
      ],
      "type": "object"
    },
    "name": "move_to_layer",
    "readOnly": false
  },
  {
    "description": "Run TrenchBroom's validators and report what is wrong with the map: missing or misconfigured entities, bad UV scales, out-of-bounds geometry and so on. The feedback loop for checking your own work.",
    "inputSchema": {
      "properties": {
        "filter": {
          "description": "Only issues whose description contains this substring.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "list_issues",
    "readOnly": true
  },
  {
    "description": "Report what occupies a point in space: solid brushwork or nothing. Useful for checking that a room's interior is open and its walls are not.",
    "inputSchema": {
      "properties": {
        "point": {
          "description": "The point to test, as [x, y, z].",
          "items": {
            "type": "number"
          },
          "maxItems": 3,
          "minItems": 3,
          "type": "array"
        }
      },
      "required": [
        "point"
      ],
      "type": "object"
    },
    "name": "point_contents",
    "readOnly": true
  },
  {
    "description": "Cast a ray and report the first thing it hits. The same test the editor uses for clicking; good for line-of-sight and \"what is in this direction\" questions.",
    "inputSchema": {
      "properties": {
        "direction": {
          "description": "Ray direction; need not be normalised.",
          "items": {
            "type": "number"
          },
          "maxItems": 3,
          "minItems": 3,
          "type": "array"
        },
        "origin": {
          "description": "Ray start, as [x, y, z].",
          "items": {
            "type": "number"
          },
          "maxItems": 3,
          "minItems": 3,
          "type": "array"
        }
      },
      "required": [
        "origin",
        "direction"
      ],
      "type": "object"
    },
    "name": "pick",
    "readOnly": true
  },
  {
    "description": "Delete objects from the map. Prefer this over the editor's Delete action, which is only available while the map view has keyboard focus.",
    "inputSchema": {
      "properties": {
        "select": {
          "description": "Finds objects already in the map. All given fields must match. Resolved fresh on every call, so undo and redo cannot invalidate it.",
          "properties": {
            "all": {
              "description": "Every object in the map.",
              "type": "boolean"
            },
            "bounds": {
              "description": "An axis-aligned box in world units. Corners may be given in either order.",
              "properties": {
                "max": {
                  "description": "The opposite corner, as [x, y, z].",
                  "items": {
                    "type": "number"
                  },
                  "maxItems": 3,
                  "minItems": 3,
                  "type": "array"
                },
                "min": {
                  "description": "One corner, as [x, y, z].",
                  "items": {
                    "type": "number"
                  },
                  "maxItems": 3,
                  "minItems": 3,
                  "type": "array"
                }
              },
              "required": [
                "min",
                "max"
              ],
              "type": "object"
            },
            "classname": {
              "description": "Entities with this classname, and the brushes belonging to them. A trailing * matches a prefix, e.g. \"func_*\".",
              "type": "string"
            },
            "group": {
              "description": "The group with this name, matched as a whole. Trailing * matches a prefix.",
              "type": "string"
            },
            "layer": {
              "description": "Objects in the named layer.",
              "type": "string"
            },
            "material": {
              "description": "Brushes with at least one face using this material. A trailing * matches a prefix.",
              "type": "string"
            },
            "mode": {
              "description": "How 'bounds' matches: fully inside it (default) or merely overlapping it.",
              "enum": [
                "contained",
                "touching"
              ],
              "type": "string"
            }
          },
          "type": "object"
        },
        "target": {
          "description": "What to act on. \"created\" is everything earlier operations in this same call produced, \"last\" is only what the previous operation produced, \"selection\" is what is selected in the editor, and \"auto\" (the default) means created-if-any, otherwise the selection. Use \"last\" to act on one earlier result, such as subtracting only the cutting brush from walls built in the same batch.",
          "enum": [
            "auto",
            "created",
            "last",
            "selection"
          ],
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "delete_objects",
    "readOnly": false
  },
  {
    "description": "Copy objects in place and leave the copies as this call's output, so a following operation moves the copy rather than the original. The usual way to repeat architecture such as columns or windows.",
    "inputSchema": {
      "properties": {
        "select": {
          "description": "Finds objects already in the map. All given fields must match. Resolved fresh on every call, so undo and redo cannot invalidate it.",
          "properties": {
            "all": {
              "description": "Every object in the map.",
              "type": "boolean"
            },
            "bounds": {
              "description": "An axis-aligned box in world units. Corners may be given in either order.",
              "properties": {
                "max": {
                  "description": "The opposite corner, as [x, y, z].",
                  "items": {
                    "type": "number"
                  },
                  "maxItems": 3,
                  "minItems": 3,
                  "type": "array"
                },
                "min": {
                  "description": "One corner, as [x, y, z].",
                  "items": {
                    "type": "number"
                  },
                  "maxItems": 3,
                  "minItems": 3,
                  "type": "array"
                }
              },
              "required": [
                "min",
                "max"
              ],
              "type": "object"
            },
            "classname": {
              "description": "Entities with this classname, and the brushes belonging to them. A trailing * matches a prefix, e.g. \"func_*\".",
              "type": "string"
            },
            "group": {
              "description": "The group with this name, matched as a whole. Trailing * matches a prefix.",
              "type": "string"
            },
            "layer": {
              "description": "Objects in the named layer.",
              "type": "string"
            },
            "material": {
              "description": "Brushes with at least one face using this material. A trailing * matches a prefix.",
              "type": "string"
            },
            "mode": {
              "description": "How 'bounds' matches: fully inside it (default) or merely overlapping it.",
              "enum": [
                "contained",
                "touching"
              ],
              "type": "string"
            }
          },
          "type": "object"
        },
        "target": {
          "description": "What to act on. \"created\" is everything earlier operations in this same call produced, \"last\" is only what the previous operation produced, \"selection\" is what is selected in the editor, and \"auto\" (the default) means created-if-any, otherwise the selection. Use \"last\" to act on one earlier result, such as subtracting only the cutting brush from walls built in the same batch.",
          "enum": [
            "auto",
            "created",
            "last",
            "selection"
          ],
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "duplicate",
    "readOnly": false
  },
  {
    "description": "Write the map to disk. Without a path this saves over the file the map was opened from. Nothing built through these tools survives closing the editor until this is called.",
    "inputSchema": {
      "properties": {
        "path": {
          "description": "Where to save. Required the first time for a map that has never been saved.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "save_map",
    "readOnly": false
  },
  {
    "description": "Undo the last step, whether it came from these tools or from the user editing by hand. Reports what was undone.",
    "inputSchema": {
      "properties": {},
      "required": [],
      "type": "object"
    },
    "name": "undo",
    "readOnly": false
  },
  {
    "description": "Redo the step that was last undone.",
    "inputSchema": {
      "properties": {},
      "required": [],
      "type": "object"
    },
    "name": "redo",
    "readOnly": false
  },
  {
    "description": "Set a key on the worldspawn entity, such as 'wad' to attach a texture WAD, or 'message' to name the level.",
    "inputSchema": {
      "properties": {
        "key": {
          "description": "Property key, e.g. \"wad\".",
          "type": "string"
        },
        "value": {
          "description": "Property value.",
          "type": "string"
        }
      },
      "required": [
        "key",
        "value"
      ],
      "type": "object"
    },
    "name": "set_worldspawn_property",
    "readOnly": false
  },
  {
    "description": "Run several operations as a single undo step. Either all of them apply or, if any fails, none do. Use this for anything built from more than one or two brushes.",
    "inputSchema": {
      "properties": {
        "name": {
          "description": "Name shown in TrenchBroom's undo history.",
          "type": "string"
        },
        "ops": {
          "description": "Operations to run in order.",
          "items": {
            "properties": {
              "params": {
                "type": "object"
              },
              "tool": {
                "type": "string"
              }
            },
            "required": [
              "tool"
            ],
            "type": "object"
          },
          "type": "array"
        }
      },
      "required": [
        "ops"
      ],
      "type": "object"
    },
    "name": "batch",
    "readOnly": false
  }
]
